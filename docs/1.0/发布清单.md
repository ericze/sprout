# sprout 发布清单（Release Checklist）

更新日期：2026-04-06
适用版本：v1（iOS 17+，SwiftUI + SwiftData + Observation）

外部平台 runbook：
[2026-04-06-signing-asc-runbook.md](/Users/ze/Documents/opc/firstgrowth/docs/plans/2026-04-06-signing-asc-runbook.md)

## 1. 当前基线状态

- [x] 代码可稳定构建与测试通过
- [x] 本地静态分析通过
- [x] 无签名 archive 预检通过
- [x] Simulator 安装与冷启动 smoke 通过
- [x] CI workflow 已接入仓库
- [x] 发布预检（手动 archive）workflow 已接入仓库

## 2. 构建配置核对

- [x] Project: `sprout.xcodeproj`
- [x] Shared Scheme: `sprout`
- [x] Targets: `sprout`、`sproutTests`
- [x] Build Configurations: `Debug`、`Release`
- [x] Bundle ID: `zd.sprout`
- [x] App target signing team in project: `B47K832US9`
- [x] Deployment target: iOS 17+
- [x] Shipping entitlements file is effectively empty

## 3. 版本号策略（必须在发版前更新）

- [x] `MARKETING_VERSION = 1.0`（当前值）
- [x] `CURRENT_PROJECT_VERSION = 1`（当前值）
- [ ] 发版前手动递增版本号策略
  - `MARKETING_VERSION`：语义化版本（例如 `1.0.1`）
  - `CURRENT_PROJECT_VERSION`：递增 build number（例如 `2`, `3`, `4`）

## 4. 权限与隐私文案

- [x] 相机权限文案（英文）已存在
- [x] 相册读取权限文案（英文）已存在
- [x] `zh-Hans.lproj/InfoPlist.strings` 已有本地化权限文案
- [x] `en.lproj/InfoPlist.strings` 已有本地化权限文案
- [ ] 发版前在 App Store Connect 隐私问卷对齐“仅本地记录照片”实际行为

## 5. 图标与启动资源

- [x] AppIcon 1024 资源存在：`icon.png`
- [x] Dark AppIcon 1024 资源存在：`darkicon.png`
- [ ] Tinted icon 槽位当前未配置文件，发版前确认是否补齐
- [ ] 启动体验（冷启动/暖启动）真机自测通过

## 6. 自动化门禁（已落地）

- [x] PR / Push 自动构建：`.github/workflows/ios-ci.yml`
- [x] PR / Push 自动测试：`.github/workflows/ios-ci.yml`
- [x] 基础静态检查（`xcodebuild analyze`）：`.github/workflows/ios-ci.yml`
- [x] 手动 archive 预检：`.github/workflows/ios-release-preflight.yml`

## 7. 回归冒烟清单（发版前必做）

- [x] Onboarding 核心状态迁移与完成路径已有自动化覆盖
- [x] 首页四条高频记录与 Undo 已有自动化覆盖
- [x] 成长录入、指标切换、图表交互核心路径已有自动化覆盖
- [x] 珍藏新增、时间线、周信、图片失败态已有自动化覆盖
- [x] 持久化异常路径：启动失败不做 destructive recovery，且已有自动化覆盖
- [ ] 设置页：语言切换、宝宝资料编辑、返回主路径人工 smoke
- [ ] 深色模式 + Dynamic Type + VoiceOver 人工 smoke
- [ ] 真机完整体验 smoke：onboarding / home / growth / treasure / settings

## 8. 发版前命令清单

- [x] 本地构建
  `xcodebuild -project sprout.xcodeproj -scheme sprout -configuration Debug -destination 'platform=iOS Simulator,name=iPhone 17,OS=latest' build CODE_SIGNING_ALLOWED=NO`
- [x] 本地测试
  `xcodebuild -project sprout.xcodeproj -scheme sprout -configuration Debug -destination 'platform=iOS Simulator,name=iPhone 17,OS=latest' test CODE_SIGNING_ALLOWED=NO`
- [x] 本地静态分析
  `xcodebuild -project sprout.xcodeproj -scheme sprout -configuration Debug -destination 'platform=iOS Simulator,name=iPhone 17,OS=latest' analyze CODE_SIGNING_ALLOWED=NO`
- [x] 本地 archive 预检（无签名）
  `xcodebuild -project sprout.xcodeproj -scheme sprout -configuration Release -destination 'generic/platform=iOS' -archivePath /tmp/sprout.xcarchive archive CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO`

## 9. 外部阻塞（最后一步，仓库外）

- [ ] Apple Developer 证书与 Provisioning Profile 配置
- [ ] Xcode Signing Team / Bundle ID / Capability 最终校验
- [ ] App Store Connect 应用元数据与隐私信息填写
- [ ] App Store Connect 隐私问卷与权限用途对齐
- [ ] 中英截图与商店素材上传
- [ ] TestFlight 上传与外部测试组发布

当第 1-8 节全部通过后，剩余工作可压缩为：  
**“使用有效签名执行 Archive -> Upload to App Store Connect -> 提交 TestFlight/审核”**。
